在这个任务中，您需要用java开发客户机和服务器。

1）编写一个并发TCP服务器，提供三个简单的服务：

（i）每当客户机发送请求时，服务器提供一个介于1之间的随机数

和10。

（二）。回音服务。

（三）。日间服务。

2个）

编写一个udp并发服务器，使文件包含一个简单的表，其中每行有两个元素：

学生证（或学号）和学生姓名。一旦客户机向服务器发送学生id，它将查找表

并回复学生姓名。
